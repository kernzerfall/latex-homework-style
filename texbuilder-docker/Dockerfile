# texbuilder Dockerfile for use with GitLab-CI
FROM archlinux:latest

# biber ends up in this weird path even though it's
# a system package
ENV PATH="$PATH:/usr/bin/vendor_perl"

# All TeXLive packages + pygments for minted + more
# The perl stuff is needed for biber
RUN pacman -Syu --noconfirm && \
	pacman -S --noconfirm bash make git zip icu \
		texlive texlive-lang texlive-meta \
		python-pygments biber \
		perl-xml-libxml perl-alien-libxml2 libxml2

#### Uncomment the following for Minion Pro support
#### You need at least MinionPro-{Bold,BoldIt,It,Regular}.otf to use with
####   fontspec/unicode-math.
#### `minionpro-texmf` should contain the files generated by the 
####   `scripts/install` step of https://github.com/sebschub/FontPro,
####   i.e. just do `mkdir minionpro-texmf && scripts/install minionpro-texmf`

## MinionPro fontspec/unicode-math
# COPY MinionPro-*.otf /usr/share/fonts/minionpro/

## MinionPro package
# COPY minionpro-texmf/ /usr/share/texmf/
# RUN mktexlsr /usr/share/texmf && updmap-sys --enable Map=MinionPro.map
